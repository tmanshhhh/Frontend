import styled from "styled-components";

export const PageContainer = styled.div`
    display: flex;
    flex-wrap: wrap;
    grid-gap: 60px;
    justify-content: start;
    padding-left: 75px;
    padding-top: 20px;
    padding-right: 75px;

`;
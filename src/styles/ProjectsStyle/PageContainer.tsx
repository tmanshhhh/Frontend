import styled from "styled-components";

export const PageContainer = styled.div`
    display: flex;
    flex-wrap: wrap;
    grid-gap: 60px;
    justify-content: start;
    width: 90%;
    margin-left: 75px;
`;
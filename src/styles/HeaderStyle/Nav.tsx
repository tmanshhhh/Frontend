import styled from "styled-components";

export const Nav = styled.nav`
    flex-direction: row;
    justify-content: center;
    float: right;
    display: flex;
    gap: 3rem;
    margin: 0 auto;
`;